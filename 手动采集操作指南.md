# 第一次操作

1. 打开 http://127.0.0.1:3100/sources，查看顶部「手动采集体验」。
2. 保持「单篇链接」，点击「填入已验证的微软文章」，再点「读取原文（暂不调用AI）」。这篇文章已做限量联调，可能直接命中缓存，不会重复访问网站。
3. 对照左侧原文，勾选文章，点击「分析所选1篇」。首次分析使用现有硅基流动 DeepSeek API；已分析的文章复用结果，不重复收费。
4. 检查中文摘要、分类、标签和影响建议。右侧是AI分析，不表示事实已独立核验。
5. 点击「确认入库」，再点「加入日报」「去生成日报」。已入库的文章显示「已入库」，不会重复保存。
6. 在报告中心确认已选文章，点「生成日报」。这篇微软文章发布时间为2026年9月1日，若按日期查找，请选「全部日期」或指定该日期。

## 想试新文章

- 单篇：输入具体文章HTTPS链接，不要输入网站首页。
- RSS：选「RSS试采集」，点击「填入微软官方博客订阅」，读取最多3篇。不会递归遍历全站。
- 网站无法读取：选「粘贴正文」，填写标题和200–12000字符正文，再预览、分析。此路径明确标记为「用户提供正文」。

## 运行边界

没有定时任务、后台轮询和自动重试。每个来源10分钟冷却，缓存文章可直接复用。同站串行请求且间隔至少1.5秒，尊重robots的更长间隔（大于15秒则停止本次演示）。每次读取最多8个网络请求，含robots、RSS和文章；每个响应最多2MB，正文最多保留12000字符。

遇到登录、验证码、robots禁止、跳转、超时会停止对应读取，不绕过限制。跳转页面需用户复制最终HTTPS地址。部分RSS文章失败会逐条显示原因。国外来源是否可用取决于本地网络；首次联调微软RSS和文章成功，Hugging Face超时。

草稿、AI结果、入库文章和操作记录位于项目 `data/collection.sqlite`，重启后保留。使用Node.js 22.13以上内置SQLite（当前环境22.22）。文件已在gitignore中排除。确认入库后，新文章与原有资讯同时保留，本地入库和原库分别最多读取100条。每条独立标记是否为示例；日报按实际选中的新闻判断数据类型。删除浏览器数据不会删除已入库文章。

发布时间无法提取时保留空值；界面明确显示采集时间，列表以采集时间辅助排序。暂不生成可信度、风险评级和AI概率评分。

来源地址清单的旧配置编辑仍属于页面配置展示，不代表已接通或持续采集；「选择并试采集」仅填入地址，必须再次点击读取。日报偏好和历史仍存当前浏览器。

## 验证

`node tests/collection.test.mjs` 验证内网限制、HTML/RSS解析、冷却、草稿和幂等入库；`node tests/daily-report.test.mjs` 验证日报接口回归。`npm run build` 检查生产构建。
