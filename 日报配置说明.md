# 定制日报

项目根目录已准备好 `.env.local`，补上 `SILICONFLOW_API_KEY` 即可。不要将真实密钥提交到版本库或发送到聊天中。

```dotenv
SILICONFLOW_API_KEY=填写你的硅基流动API密钥
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V4-Pro
```

保存后重新启动 Next.js 服务（开发模式 `npm run dev`；生产模式先 `npm run build` 再 `npm start`）。模型名按用户提供的值预设，最终以硅基流动账户可调用的模型 ID 为准；出现模型不存在或权限错误时核对控制台。

## 体验流程

1. 首页“挑选新闻加入日报”或资讯库详情中的“加入日报”可添加待选新闻。
2. 打开报告中心，按分类、标签、日期筛选。默认最近24小时，示例新闻过期时手动选择“全部日期”。指定日期按北京时间计算。
3. 分类和标签之间取交集，各组内取并集；不选则不限。改变筛选条件会重新精选新闻，生成前需确认清单。手动搜索可从全部日期补充新闻。
4. 选择1–30条新闻，点击生成日报。服务端重新读取这些新闻，仅向硅基流动发送必要素材；每条摘录和摘要分别最多1800和1200字符。正文逐步显示，只有完整成功后才保存。取消、超时、限流、空回复及中断不会覆盖上次成功记录。
5. 完整日报可复制并包含来源；首页显示最新成功日报摘要。偏好、待选新闻和最近20份历史保存在当前浏览器，不跨设备同步，没有账户隔离，也不写入数据库 reports 表。清除站点数据会清除这些记录。

未配置数据库或数据库读取失败时，现有仓库会回退到示例新闻。界面和模型提示均标注示例数据。真实模型调用不等于接通实时新闻采集。当前新闻查询沿用现有仓库最近100条范围。

未配置API Key时可以体验筛选和选择，生成按钮会禁用并提示配置。服务端不会把密钥返回浏览器。没有密钥时无法验证真实模型权限、余额、速度及结果质量。

此版本用于当前单浏览器/本地体验。若以后公开部署给多人使用，需先补充调用额度、访问控制及持久化设计。
